"use strict";

describe( "Directive: engoDelegateTo", function () {
  var element, scope;

  beforeEach( module( "engoPupil" ));
  beforeEach( inject( function ( $rootScope ) {
    scope = $rootScope.$new();
  }));

});
