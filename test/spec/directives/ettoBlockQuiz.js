"use strict";

describe("Directive: ettoBlockQuiz", function () {
  var element, scope;

  beforeEach(module("ettoPupil"));

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));
  /*
  it("should return derp",
    function () {
      console.log("---------------------", scope.returnDerp());
      expect(scope.returnDerp()).toEqual(1);
    }
  );
*/
});
