"use strict";

describe( "Directive: ettoDelegateTo", function () {
  var element, scope;

  beforeEach( module( "ettoPupil" ));
  beforeEach( inject( function ( $rootScope ) {
    scope = $rootScope.$new();
  }));

});
