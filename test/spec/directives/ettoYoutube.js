"use strict";

describe("Directive: ettoYouTube", function () {

  // load the directive's module
  beforeEach(module("ettoPupil"));

  var element,
    scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));

});

