nav.navbar.navbar-default( role="navigation", etto-session)
  .navbar-header
    button.navbar-toggle( data-toggle="collapse", data-target="#etto-navbar" )
      .sr-only Toggle navigation
      .icon-bar
      .icon-bar
      .icon-bar
    a.navbar-brand( href="/etto" )
      span
        img(src="/images/site/logo.png")
      span.logo-name Coursettoetto

  #etto-navbar.collapse.navbar-collapse
    ul.nav.navbar-nav
      li
        a( href="/etto" ) Home
      li
        a( href="/store/{{user._tier._id}}" ) Store
      li
        a( href="/levels/{{user._tier._id}}" ) Reports
      li
        a( href="/settings/{{user._id}}" ) Settings

    .navbar-right
     a.btn.btn-default(href="/api/v1/auth/logout", target="_self")
      i.fa.fa-times-circle.fa-1x

    //
      button(class="btn dropdown-toggle sr-only", type="button", id="dropdownMenu1", data-toggle="dropdown") Dropdown
        span.caret
      ul.dropdown-menu(role="menu", aria-labelledby="dropdownMenu1")
        li(role="presentation")
          a(role="menuitem", tabindex="-1", href="#") Action
        li(role="presentation")
          a(role="menuitem", tabindex="-1", href="#") what
        li(role="presentation")
          a(role="menuitem", tabindex="-1", href="/api/v1/auth/logout", target="_self") Logout




